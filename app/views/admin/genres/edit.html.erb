<%= form_with model: [:admin, @genre_edit_target] do |form_g_e_t| %>
  <div class = "container">
    <div class = "row">
      <div class = "col-lg-3">
        <h4>ジャンル編集</h4>
      </div>
    </div>

    <div class = "row">
      <div class = "col-lg-2">
        ジャンル名
      </div>

      <div class = "col-lg-3">
        <%= form_g_e_t.text_field :name %>
      </div>

      <div class = "col-lg-2">
        <%= form_g_e_t.submit "変更を保存", class: "btn btn-success"%>
      </div>
    </div>

  </div>

<% end %>
